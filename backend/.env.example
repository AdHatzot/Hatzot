PORT=3000
CLIENT_ORIGIN=http://localhost:5173

# Database — leave unset until TypeORM lands (see src/db/data-source.ts).
# Without it the backend serves in-memory seed data.
# DB_URL=postgres://user:pass@localhost:5432/c2
