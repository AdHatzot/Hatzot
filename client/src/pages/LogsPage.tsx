/**
 * @team     core
 * @owner    core
 * @public   no
 * @updated  2026-09-08
 *
 * Route stub. The page itself belongs to the loop team.
 */
import { LogsView } from '@/features/loop';

export function LogsPage(): JSX.Element {
  return <LogsView />;
}
